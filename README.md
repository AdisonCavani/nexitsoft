## Development

Create database (docker):

```bash
docker compose up -d
```

Run the application:

```bash
cd src
dotnet run
```

Open the application in the browser at [localhost:5045](http://localhost:5043/)
